# Changelog

## Unreleased

- New desktop conversations remember the last model explicitly selected in the
  Composer and restore reasoning preferences separately for each provider/model.
  Unavailable models fall back to Settings; permissions keep their global default.

Released, localized in-app notes remain in `packages/shared/src/changelog.ts`.
